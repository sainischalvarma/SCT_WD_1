# Responsive Landing Page

A beautiful, modern, responsive landing page — Aura Creative Studio.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
